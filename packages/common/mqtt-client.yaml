defaults:
  name: ESPHome

# See https://esphome.io/components/mqtt/
mqtt:
  id: mqtt_id
  discovery: false # disable entity discovery
  discover_ip: true # enable device discovery
  topic_prefix: "esphome/${name}"
